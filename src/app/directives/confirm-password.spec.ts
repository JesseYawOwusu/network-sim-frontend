import { ConfirmPassword } from './confirm-password';

describe('ConfirmPassword', () => {
  it('should create an instance', () => {
    const directive = new ConfirmPassword();
    expect(directive).toBeTruthy();
  });
});
